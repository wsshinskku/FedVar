# Preserved source

`FedVar_original.py` is the unchanged source from the earlier repository revision. It is retained for provenance and historical comparison. Install the root package and use `fedvar train` for the maintained implementation; see [the algorithm maintenance notes](../docs/ALGORITHM.md#maintenance-changes).
